{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "python -m venv venv"
      }
    },
    {
      "method": "shell.run",
      "params": {
        "venv": "venv",
        "message": "pip install -r requirements.txt"
      }
    },
    {
      "method": "fs.link",
      "params": {
        "venv": "venv"
      }
    }
  ]
}
